
This module could join `vertical-association` if:
- reviewal process does not rely on Search Engine pausing-trick
- move-in logic of `is_publish` from `oca_search_engine`
- it adds route and template/view on native Odoo website to adds
  basic sponsor pages (similar to v14.0 in `website_oca_integrator`)
- cleaning of other OCA customization:
    - countries: to keep?
    - industries: to remove?
